#include "read.h"
#include <stdio.h>
#include <stdlib.h>

void run_read()
{
    // char name[10];
    // int age;
    // printf("say hello\n");
    // // fgets(name, 10, stdin);
    // int res = scanf("%10s", name);
    // printf("res is %d\n", res);
    // printf("Name is %s age is %d\n", name, age);
}
